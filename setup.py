from setuptools import setup, find_packages

setup(
    name="email-agent",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Your dependencies from requirements.txt will go here
    ],
)
